import React, { Component } from "react";
// import './styles.scss';

export default class Dashboard extends Component {
  render() {
    return (
      <div>
        this is a dashboard
      </div>
    );
  }
}